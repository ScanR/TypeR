{
  "name": "typer-mcp",
  "version": "2.0.0",
  "description": "MCP stdio server that proxies commands to the TypeR HTTP bridge running inside the Photoshop CEP panel.",
  "type": "module",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node test/smoke.js"
  }
}
